var GKAsync = require('../lib/gkasync');

var async = new GKAsync();
async.sequence([
        function(callback) {
            console.log('first job');
            callback('err', 'second');
        },
        function(arg1, callback) {
            console.log(arg1 + ' job');
            var sys = require('sys');
            setTimeout(function() {
                callback(null, 'third');
            }, 1000);
        },
        function(arg1, callback) {
            console.log(arg1 + ' job');
            callback(null, 'done');
        }
], function(err, result) {
    console.log("result: " + err);
    console.log("result: " + result);
});

var arr = new Array();
arr.push("test1");
arr.push("test2");
arr.push("test3");
var sum = function(data) {
};
async.forEach(arr, sum, function(err) {
  console.log('done');
});
